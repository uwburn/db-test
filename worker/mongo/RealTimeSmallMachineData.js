"use strict";

module.exports = require(`./RealTimeMachineData`)(`Small`, {
    counters: `recordTimeComplex`,
    setup: `recordTimeComplex`,
    mng: `recordTimeComplex`,
    geo: `recordTimeComplex`,
    alarm: `recordInterval`,
});
