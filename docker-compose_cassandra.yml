version: '3'

services:
  cassandra:
    image: cassandra
    ports:
      - "9042:9042"
    volumes:
      - cassandra_data:/var/lib/cassandra
  coordinator:
    environment:
      CONFIG: "cassandra_machine_small.json"

volumes:
  cassandra_data: